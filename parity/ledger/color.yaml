module: packages/core/src/color.ts
status: partial
baseline: 4.2.6
upstream:
  - path: src/z-color.h
    items: [COLOUR_* constants (all 29), MAX_COLORS]
  - path: src/z-color.c
    items:
      - angband_color_table (RGB values; leading mode byte dropped)
      - color_table index chars and names
      - color_char_to_attr / color_text_to_attr
notes: >-
  Pending: the color_table translation matrix (mono/vga/blind/lighter/
  darker/highlight/metallic/misc) used by lighting and accessibility
  modes; port it alongside the modules that consume it. colorToCss is a
  port-only front-end convenience.
verified-by:
  - unit tests (order, RGB spot checks, unique attr chars, lookups)
