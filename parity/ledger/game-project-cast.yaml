module: packages/core/src/game/project-cast.ts
status: verified
baseline: 4.2.6
upstream:
  - path: src/effect-handler-attack.c
    items:
      - get_target (L38): the portable branches - SRC_PLAYER (DIR_TARGET at an
        acquired target, else the adjacent grid in the direction) and the
        default / SRC_NONE case (target the player, set PROJECT_PLAY). Ported as
        resolveAimedTarget. The SRC_MONSTER confused-direction, target-monster
        (monster_target_monster) and decoy (cave_find_decoy) branches are
        handled by monster-spell targeting (game/mon-cast.ts) and the decoy effect
    (effect-general.ts:199); #19
        resolves those and passes the grid in.
      - project_aimed (L94): the PROJECT_THRU add + get_target flag merge, folded
        into castBolt / castBeam.
      - effect_handler_BOLT (L127): castBolt (PROJECT_STOP | KILL | THRU, plus
        PROJECT_PLAY for a non-player source).
      - effect_handler_BEAM (L143): castBeam (PROJECT_BEAM | KILL | THRU, plus
        PROJECT_PLAY for a non-player source).
      - effect_handler_BALL (L594): castBall - the flag toggles (a monster source
        adds PROJECT_PLAY and drops STOP/THRU; a player source drops STOP/THRU
        only when a specific target was acquired) and the radius default of 2.
        The powerful-monster (+1 rad) and player-level (rad += lev / other)
        radius bonuses are applied by the caller (they need monsterIsPowerful /
        player.lev, both available there); the confused-target and decoy
        branches follow get_target, which the shell resolves (target-loop.ts).
      - effect_handler_LINE (L173): castLine (PROJECT_BEAM | GRID | KILL | THRU).
      - effect_handler_ALTER (L186): castAlter (PROJECT_BEAM | GRID | ITEM |
        THRU, no damage, no KILL).
      - effect_handler_ARC (L789): castArc - PROJECT_ARC | GRID | ITEM | KILL,
        the min-20 arc width, rad 0 => max_range, and the source-diameter
        widening (4 * 60 / degrees, capped 25) for cones under 60 degrees.
      - effect_handler_BREATH (L681): castBreath - as castArc plus the
        powerful-monster full-strength boost (source diameter 4 -> 6 before the
        arc widening); breath_dam and the "You breathe" message are the caller's
        (#19).
      - effect_handler_SHORT_BEAM (L852): castShortBeam - a fixed-length beam
        (PROJECT_ARC, degrees 0, source diameter = radius) normalised to a beam
        by project(); the player-level radius bonus is the caller's.
      - effect_handler_SPOT (L545): castSpot - explode on the player's grid
        (PROJECT_STOP | PLAY | GRID | ITEM | KILL | SELF, source diameter = rad).
      - effect_handler_SPHERE (L571): castSphere - a ball from the player's grid
        with an explicit source diameter (PROJECT_STOP | GRID | ITEM | KILL).
      - effect_handler_STRIKE (L1001): castStrike - a jump ball on the target
        (PROJECT_JUMP | GRID | ITEM | KILL).
      - effect_handler_STAR (L1032): castStar - a beam in each of the eight
        compass directions (PROJECT_THRU | BEAM | GRID | KILL).
      - effect_handler_STAR_BALL (L1062): castStarBall - a ball in each of the
        eight compass directions (PROJECT_STOP | THRU | GRID | ITEM | KILL).
      - project_touch (L112) / effect_handler_TOUCH (L411): castTouch - the base
        player-centred radius-1 ball (PROJECT_GRID | KILL | HIDE | ITEM | THRU,
        +AWARE when aware); the monster decoy / target-monster branches deferred.
      - effect_handler_SWARM (L974): castSwarm - fire value.m_bonus balls at a
        fixed target grid (PROJECT_THRU | STOP | GRID | ITEM | KILL).
      - effect_handler_PROJECT_LOS (L1088): castProjectLos - a jump projection to
        every monster in LOS of the origin, always attributed to the player,
        excluding the currently-acting monster (cave->mon_current).
  - path: src/player-util.c
    items:
      - op_ptr->hitpoint_warn default (option.c L205 = 3): DEFAULT_HITPOINT_WARN,
        used by basicPlayerActor until the options system lands.
notes: >-
  The projection-casting spine: the game-layer glue that makes the whole
  projection stack usable. castProjection assembles project()'s ProjectHooks
  (world/project.ts) from the two drivers - projectMonster
  (game/project-monster.ts) and projectPlayer (game/project-player.ts) - over
  the live GameState, and runs one projection. It is the single seam the attack
  effect handlers (#18), monster spells (#19) and player spellcasting (#22) all
  dispatch a bolt/beam/ball/arc/breath through. The flag-shape helpers
  (castBolt / castBeam / castBall / castArc / castBreath / castShortBeam /
  castLine / castAlter / castSpot / castSphere / castStrike / castStar /
  castStarBall / castTouch / castSwarm / castProjectLos) preset project()'s
  flags and radius exactly as the matching effect_handler_* and delegate to it.

  The player-projection actor (PlayerProjActor: resist levels, damage
  reduction, minus_ac) is supplied by the caller, exactly as PlayerActor's
  combat / defense views are, because calc_bonuses (which derives el_info,
  dam_red, perc_dam_red, minus_ac) is all computed (player/calcs.ts,
    game/gear.ts minusAc) and carried by the live actor. basicPlayerActor builds the
  no-resist, no-reduction view over the live Player for callers and tests until
  it lands; chp and is_dead are live getters/setters so take_hit's mutations
  write back to the player and the game state.
deferred:
  - effect_handler_LASH (L899) IS ported, in the module that has the blow data -
    game/effect-attack.ts:356, summing the full first blow plus half of every
    other, with lash_type off the first blow - and it casts through this
    module's short beam.
  - CLOSED (2026-08-07). "Next increment" arrived: the EF_* handlers are
    registered (effects/handlers.ts plus the game-layer registrars) and the
    player-timed adapter is game/effect-env.ts buildEffectContext, which
    session/game.ts wires with the live timed table, incQueries and incHooks.
  - resolveAimedTarget's monster branch and the real target system are ported
    (game/effect-attack.ts, game/target.ts); env.aimed is backed by the live
    target rather than a stand-in.
  - All three are supplied: onObject and onFeature are wired at
    game/project-cast.ts:341-344 to projectObject / projectFeature over the live
    floor piles and terrain (whenever a world env exists), and breath_dam is
    mon/spell.ts breathDam, read at game/effect-attack.ts:224.
  - CLOSED (2026-08-07). It does: session/game.ts:1295 builds the live actor's
    resistLevel from derived.elInfo (the calc_bonuses output) and :1304 supplies
    minusAc for real. basicPlayerActor's zeroes are the WORLDLESS default that
    keeps harness tests total, not what the game runs on.
verified-by:
  - game/project-cast.test.ts (23 tests): basicPlayerActor writes take_hit
    mutations back to the live player/state and shares the timed array;
    resolveAimedTarget aims a player projection at the adjacent grid, at an
    acquired DIR_TARGET grid, and a monster projection at the player with
    PROJECT_PLAY; castBolt damages the monster it stops at, tracks the single
    monster hit, damages the player through the live actor, kills the player and
    fires the death hook, and suppresses vs shows bolt visuals by blindness;
    castBeam passes through and damages every monster in line; castBall explodes
    on a player-cast target and detonates on the player for a monster source;
    castLine damages a monster in its path while castAlter (no KILL) leaves it
    unharmed; castArc damages a monster on its centreline; castSpot hits a
    neighbour and scales self damage down; castStar shoots a beam in each
    direction; castStrike drops a jump ball with no path; castSwarm applies its
    given shot count; castProjectLos hits every monster in LOS, skips the
    excluded caster, and does not reach one blocked by a wall.
